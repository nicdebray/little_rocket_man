<h1>All bookings for <%= @rocket.name %> rocket </h1>

<% @rocket.bookings.each do |booking| %>
<ul>
  <li><%= booking.departure_date %></li>
  <li><%= link_to "Delete", rocket_booking_path(@rocket.id, booking.id),
  method: :delete,
  data: { confirm: "Are you sure?"} %></li>
</ul>
<% end %>

